### Phase 1: Naive RAG

Pipeline details:
- chunk size 1000 chars & overlap 200
- Dense only rag (openai's text embedding 3 small model)
- Number of retrives top_k = 5
- No version filter
- gpt 4o mini for answering
- Qdrant collection -> count = 3248 chunks

I asked 5 questions and here is my observation.

## Q1. "how do I convert a model to a dict in v2?"
This question explicitly mentions v2. So, it is a contamination bait.

The true answer, `model_dump()` does not appear in any chunks retrived. Generation produced answers faithfully although it is wrong.

**Ground Truth:**
`model.model_dump()` (canonical v2 method; recursive, supports include/exclude/by_alias). `model_dump_json()` for JSON string.

Retrieved (id | version | Score)
both:docs/migration.md:012                (both)  0.5564
v2:docs/concepts/serialization.md:007     (v2)    0.5458
v2:pydantic/main.py:077                   (v2)    0.5455
v1:docs/usage/exporting_models.md:000     (v1)    0.5434
v1:docs/usage/models.md:003               (v1)    0.5272

Answer Given:
"In v2, you can convert a model to a dictionary by using the `dict()` function. For example, you can call `dict(m)` on your model instance `m`."

**Verbatim evidence block:**
model_dump does appear in the retrieved context but it appears only as unexplained code inside a JSON-formatting comparison — no sentence saying "use model_dump() to convert a model to a dict."

**Version precision:**
2/5 chunks are v1 even though v2 mentioned in query.

**Failure/behavior:**
- The model did NOT take the v1 bait (.dict()) despite it being in context. It answered dict(m) — which works in v2 but is wrong-by-omission: shallow conversion

