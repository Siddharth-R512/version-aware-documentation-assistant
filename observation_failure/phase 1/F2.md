## Q2. "how do I write a validator in v2?"

**Ground Truth**:
V2 validators are written with `@field_validator('field_name', mode='before'|'after'|'wrap'|'plain')` as a classmethod (or `@model_validator` for whole-model logic). `@validator` is deprecated.

Retrieved (id | version | Score)
v2:pydantic/deprecated/class_validators.py:002  (v2)  0.5154
v2:HISTORY.md:399  (v2)  0.5127
v1:HISTORY.md:105  (v1)  0.5120
v2:pydantic/functional_validators.py:016  (v2)  0.5041
v2:pydantic/functional_validators.py:017  (v2)  0.4987

**Answer Given:** "I don't know based on the provided context."

**Verbatim evidence block:**
It was retrieved: `v2:pydantic/functional_validators.py:017` contains `FieldValidatorModes: TypeAlias = Literal['before', 'after', 'wrap', 'plain']` and the `field_validator(field, /, *fields, mode=...)` overload signatures. The answer's name and API shape are literally in context.. just not as prose.

**Version precision:**
3/5 chunks are noise. They are misleading. path version not equal to semantic version of the content.
Chunk 1: v2:pydantic/deprecated/class_validators.py:002 ranked highest. It prioritized v2 documentation. Version-wise retrieval is correct. But chunk is from `deprecated` directory that contains functions for backward compatibility. It has v1 related functions under v2 tree.
Chunk 2 and 3: are changelogs entries. Retrieved twice because verion is "both".

**Failure/behavior:**
- Instead of routing to `docs/concepts/validators.md` for "How do I" question, it landed on source files and changelogs due to the token density of "validator"
- The model declined the synthesis of answer from API signatures.

