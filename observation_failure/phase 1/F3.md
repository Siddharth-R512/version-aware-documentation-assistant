## Q3. "what does orm_mode do?"

**Ground Truth**:
V1-only: `Config.orm_mode = True` enables `from_orm()` to populate a model from attribute access on arbitrary objects (via `GetterDict`). Removed in v2 — replaced by `ConfigDict(from_attributes=True)` + `model_validate`.

Retrieved (id | version | Score)
v1:docs/usage/models.md:006  (v1)  0.5265
v1:pydantic/mypy.py:012  (v1)  0.5260
v1:pydantic/mypy.py:011  (v1)  0.5121
v1:pydantic/mypy.py:040  (v1)  0.4756
v1:docs/usage/models.md:007  (v1)  0.3869

**Answer Given:** The `orm_mode` must be set to `True` to enable the use of the special constructor `from_orm` to create the model instance from ORM instances. It allows Pydantic models to work with ORM objects, indicating that the model can be populated using the attributes of the ORM instance.

**Verbatim evidence block:**
`v1:docs/usage/models.md:006`: property `orm_mode` must be `True` and `from_orm` must be used - supports the answer as given.

**Version precision:**
All 5 chunks are from v1. 

**Failure/behavior:**
- Retrieval had zero cross-version diversity, and the generator wasn't prompted to scope claims.
- Soft failure: factually right for v1, silently wrong-by-omission for a v2 user. 

